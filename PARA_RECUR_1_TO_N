#include <iostream>
using namespace std;

void sum(int start,int end)
{
  if(start>end)
  {
    return;
  }
  cout<<start<<endl;
  sum(start+1,end);
}
int main()
{
    int start=1;
  sum(start,5);
    return 0;
}
