class Solution {
public:
    int largestElement(vector<int>& nums) {
  int maximum = INT_MIN;
        int n=nums.size();
        for (int i = 0; i < n; i++) {
            if (nums[i] > maximum) {
                maximum = nums[i];
            }
        }
        return maximum;
    }
};

