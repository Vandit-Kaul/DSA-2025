#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> nums={1,2,7,8,5,4};
    int i=0;
    
    while(i<nums.size()-1)
    {
        swap(nums[i],nums[i+1]);
        i+=2;
    }
    
for( int i=0;i<nums.size();i++)
{
    cout<<nums[i]<< " ";
}

}
