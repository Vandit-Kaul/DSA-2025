#include <bits/stdc++.h>
using namespace std;
int power(int x)
{
    if (x == 0)
    {
        return 1;
    }

    return 2 * power(x - 1);
}
int main()
{
    int x;

    cout << "enter the exponent:" << endl;
    cin >> x;
    cout << "the answer is:" << power(x) << endl;
    return 0;
}
