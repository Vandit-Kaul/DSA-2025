#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> nums={0,1,1,0,1,0};
    int number=0;
    
   
    
for( int i=0;i<nums.size();i++)
{
    if(nums[i]!=1){
    nums[number++]=nums[i];
    }
}

for(int i=number;i<nums.size();i++)
{
    nums[i]=1;
}

for(int i=0;i<nums.size();i++)
{
    cout<<nums[i]<<" ";
}
}
